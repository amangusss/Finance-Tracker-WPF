<Window x:Class="Finance_Tracker_WPF_API.UI.Views.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Finance_Tracker_WPF_API.UI.Views"
        xmlns:vm="clr-namespace:Finance_Tracker_WPF_API.UI.ViewModels"
        xmlns:lvc="clr-namespace:LiveChartsCore.SkiaSharpView.WPF;assembly=LiveChartsCore.SkiaSharpView.WPF"
        mc:Ignorable="d"
        Title="Finance Tracker" Height="800" Width="1200"
        Background="{StaticResource BackgroundBrush}">
    
    <Window.DataContext>
        <vm:MainViewModel/>
    </Window.DataContext>

    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <StackPanel Grid.Row="0" Orientation="Horizontal" Margin="0,0,0,20">
            <TextBlock Text="Finance Tracker" 
                     FontSize="24" 
                     FontWeight="Bold"
                     Foreground="{StaticResource PrimaryBrush}"/>
            <TextBlock Text="{Binding Balance, StringFormat='Balance: {0:C}'}"
                     FontSize="20"
                     Margin="20,0,0,0"
                     VerticalAlignment="Center"/>
        </StackPanel>

        <!-- Controls -->
        <Grid Grid.Row="1" Margin="0,0,0,20">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>

            <DatePicker Grid.Column="0" 
                      SelectedDate="{Binding StartDate}"
                      Margin="0,0,10,0"/>
            <DatePicker Grid.Column="1" 
                      SelectedDate="{Binding EndDate}"
                      Margin="0,0,10,0"/>
            <ComboBox Grid.Column="2" 
                     ItemsSource="{Binding AvailableCurrencies}"
                     SelectedItem="{Binding SelectedCurrency}"
                     Width="100"
                     Margin="0,0,10,0"/>
            <Button Grid.Column="3" 
                    Content="Add Transaction"
                    Command="{Binding AddTransactionCommand}"
                    Margin="0,0,10,0"/>
            <Button Grid.Column="5" 
                    Content="Refresh"
                    Command="{Binding LoadDataCommand}"/>
        </Grid>

        <!-- Main Content -->
        <Grid Grid.Row="2">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <!-- Transactions List -->
            <DataGrid Grid.Column="0"
                     ItemsSource="{Binding Transactions}"
                     Margin="0,0,10,0">
                <DataGrid.Columns>
                    <DataGridTextColumn Header="Date" 
                                      Binding="{Binding Date, StringFormat=d}"/>
                    <DataGridTextColumn Header="Type" 
                                      Binding="{Binding Type}"/>
                    <DataGridTextColumn Header="Amount" 
                                      Binding="{Binding Amount, StringFormat=C}"/>
                    <DataGridTextColumn Header="Description" 
                                      Binding="{Binding Description}"/>
                    <DataGridTextColumn Header="Category" 
                                      Binding="{Binding Category.Name}"/>
                    <DataGridTemplateColumn>
                        <DataGridTemplateColumn.CellTemplate>
                            <DataTemplate>
                                <Button Content="Delete"
                                        Command="{Binding DataContext.DeleteTransactionCommand, 
                                                  RelativeSource={RelativeSource AncestorType=Window}}"
                                        CommandParameter="{Binding}"/>
                            </DataTemplate>
                        </DataGridTemplateColumn.CellTemplate>
                    </DataGridTemplateColumn>
                </DataGrid.Columns>
            </DataGrid>

            <!-- Charts -->
            <GridSplitter Grid.Column="1" 
                         Width="5" 
                         HorizontalAlignment="Center" 
                         VerticalAlignment="Stretch"/>

            <Grid Grid.Column="2">
                <Grid.RowDefinitions>
                    <RowDefinition Height="*"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>

                <!-- Balance Chart -->
                <lvc:CartesianChart Grid.Row="0"
                                   Series="{Binding Transactions, Converter={StaticResource BalanceLineChartConverter}}"
                                   Margin="0,0,0,10"/>

                <!-- Category Chart -->
                <lvc:PieChart Grid.Row="1"
                             Series="{Binding CategoryTotals, Converter={StaticResource CategoryPieChartConverter}}"/>
            </Grid>
        </Grid>

        <!-- Status Bar -->
        <StatusBar Grid.Row="3" Margin="0,10,0,0">
            <StatusBarItem>
                <TextBlock>
                    <TextBlock.Text>
                        <MultiBinding StringFormat="Exchange Rate: 1 USD = {0:N2} {1}">
                            <Binding Path="ExchangeRate"/>
                            <Binding Path="SelectedCurrency"/>
                        </MultiBinding>
                    </TextBlock.Text>
                </TextBlock>
            </StatusBarItem>
        </StatusBar>
    </Grid>
</Window> 